<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gsb.alexa.gsbappli">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/gsb_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity">

        </activity>
        <activity android:name=".ReportActivity">

        </activity>
        <activity android:name=".DoctorActivity">

        </activity>
        <activity android:name=".AddDoctorActivity">

        </activity>
        <activity android:name=".AddRepportActivity">

        </activity>
        <activity android:name=".DoctorDetailActivity">

        </activity>

    </application>

</manifest>